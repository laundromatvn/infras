# Redis configuration for lms_redis (image: redis:7-alpine)
# Note: The official Redis image does not parse env vars by default.
# These are provided as samples and for potential custom entrypoints.

# If you plan to enforce a password, wire this into the command or a config file.
REDIS_PASSWORD=NmPJcR909CUt
REDIS_PORT=6379
# REDIS_APPENDONLY=yes


