apiVersion: v1
kind: Service
metadata:
  name: lms-kiosk-frontend-clusterip
  namespace: lms
  labels:
    app: lms-kiosk-frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app: lms-kiosk-frontend


